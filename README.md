Windows app does not create an app icon on the Desktop by default.
The associated script creates an app Icon on the logged in users desktop. 
This si achieved by Using an intune remidiation script which in turn pull this script and runs it as the logged on user.
